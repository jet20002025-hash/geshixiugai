# Supabase Storage 找不到 Bucket 解决方案

## 🔍 问题

在 Supabase Dashboard 中找不到 "Storage" 或 "Bucket" 选项。

---

## ✅ 解决方案

### 方法 1：检查左侧菜单

1. **登录 Supabase Dashboard**：https://supabase.com/dashboard
2. **选择你的项目**（如果还没有项目，先创建项目）
3. **查看左侧菜单**，找到以下选项之一：
   - **"Storage"**（存储）
   - **"Object Storage"**（对象存储）
   - **"Files"**（文件）

**注意**：不同版本的 Supabase 界面可能略有不同。

---

### 方法 2：直接访问 Storage URL

如果左侧菜单找不到，可以尝试直接访问：

1. 在浏览器地址栏，你的项目 URL 应该是：
   ```
   https://supabase.com/dashboard/project/你的项目ID
   ```

2. 在 URL 后面添加 `/storage`：
   ```
   https://supabase.com/dashboard/project/你的项目ID/storage
   ```

3. 按回车键，应该会直接跳转到 Storage 页面

---

### 方法 3：检查项目是否已创建

**如果看到 "No projects" 页面**：

1. **点击 "+ New project"** 按钮
2. 填写项目信息：
   - **Name**：`word-formatter`（任意名称）
   - **Database Password**：设置密码（请保存好）
   - **Region**：选择 `Southeast Asia (Singapore)` 或 `Northeast Asia (Tokyo)`
3. 点击 **"Create new project"**
4. 等待项目创建完成（约 1-2 分钟）
5. 项目创建完成后，左侧菜单会出现 **"Storage"** 选项

---

### 方法 4：使用 API 创建 Bucket（高级）

如果界面找不到，可以使用 Supabase API 直接创建：

1. **获取 API Key**：
   - Settings → API → `service_role` secret key

2. **使用 curl 命令创建 Bucket**：
   ```bash
   curl -X POST 'https://你的项目ID.supabase.co/storage/v1/bucket' \
     -H 'apikey: 你的service_role_key' \
     -H 'Authorization: Bearer 你的service_role_key' \
     -H 'Content-Type: application/json' \
     -d '{
       "name": "word-formatter-storage",
       "public": false
     }'
   ```

   将以下内容替换为实际值：
   - `你的项目ID`：在 Supabase Dashboard URL 中可以看到
   - `你的service_role_key`：Settings → API → service_role secret key

---

## 📋 完整步骤（从头开始）

### 步骤 1：创建项目

1. 访问：https://supabase.com/dashboard
2. 如果看到 "No projects"，点击 **"+ New project"**
3. 填写信息：
   - **Name**：`word-formatter`
   - **Database Password**：设置密码
   - **Region**：`Southeast Asia (Singapore)`
4. 点击 **"Create new project"**
5. 等待创建完成

### 步骤 2：找到 Storage

项目创建完成后：

1. **查看左侧菜单**，应该有以下选项：
   - Table Editor
   - Authentication
   - **Storage** ← 这个！
   - SQL Editor
   - Database
   - Edge Functions
   - Settings

2. **点击 "Storage"**

3. 如果还是找不到，尝试：
   - 刷新页面（F5 或 Cmd+R）
   - 检查浏览器控制台是否有错误
   - 尝试使用不同的浏览器

### 步骤 3：创建 Bucket

1. 进入 Storage 页面后，应该看到：
   - **"Create a new bucket"** 按钮，或
   - **"New bucket"** 按钮，或
   - **"+"** 按钮

2. 点击按钮，填写：
   - **Name**：`word-formatter-storage`
   - **Public bucket**：**取消勾选**（私有）

3. 点击 **"Create bucket"**

---

## 🆘 如果仍然找不到

### 检查项目状态

1. **确认项目已创建**：
   - 在 Dashboard 中应该能看到你的项目
   - 项目状态应该是 "Active"

2. **检查权限**：
   - 确保你是项目的所有者
   - 如果是团队成员，可能没有 Storage 权限

3. **联系支持**：
   - Supabase 支持：https://supabase.com/support
   - 或使用 Discord：https://discord.supabase.com

---

## 💡 替代方案

如果 Supabase Storage 确实无法使用，可以考虑：

### 方案 1：Backblaze B2（推荐）

- **免费额度**：10 GB 存储，1 GB 下载/天
- **配置简单**：注册后创建 Bucket 即可
- **详细步骤**：查看 `存储方案替代方案.md`

### 方案 2：七牛云（国内）

- **免费额度**：10 GB 存储，10 GB 流量/月
- **国内访问快**
- **需要实名认证**

---

## 📝 快速检查清单

- [ ] 项目已创建并处于 Active 状态
- [ ] 已登录 Supabase Dashboard
- [ ] 左侧菜单中有 "Storage" 选项
- [ ] 已进入 Storage 页面
- [ ] 能看到 "Create bucket" 或类似按钮

---

## 🎯 下一步

如果找到了 Storage 并创建了 Bucket，继续：

1. **获取 API Key**：Settings → API → service_role secret key
2. **配置 Vercel 环境变量**（参考 `解决文档不存在问题-配置Supabase.md`）

如果还是找不到，告诉我具体看到了什么，我会进一步帮你！

