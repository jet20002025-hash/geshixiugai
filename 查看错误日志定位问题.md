# 查看错误日志定位问题

## 🔍 问题

服务启动失败，状态码 3/NOTIMPLEMENTED。需要查看错误日志来定位具体问题。

---

## ✅ 排查步骤

### 步骤 1：查看错误日志

```bash
# 查看错误日志的最后部分
sudo tail -n 100 /var/log/geshixiugai/error.log

# 或者查看完整错误日志
sudo cat /var/log/geshixiugai/error.log | tail -n 100
```

### 步骤 2：查看服务日志

```bash
# 查看 systemd 服务日志
sudo journalctl -u geshixiugai -n 50 --no-pager
```

### 步骤 3：手动测试启动（查看实时错误）

```bash
cd /var/www/geshixiugai
source venv/bin/activate
gunicorn -c gunicorn_config.py backend.app.main:app
```

---

## 🔧 常见错误和解决方案

### 错误 1：缺少依赖包

如果看到 `ModuleNotFoundError` 或 `ImportError`：

```bash
cd /var/www/geshixiugai
source venv/bin/activate
pip install -r requirements.txt
```

### 错误 2：存储配置错误

如果看到 OSS 相关错误，检查 `.env` 文件：

```bash
cat /var/www/geshixiugai/.env
```

### 错误 3：Python 路径问题

如果看到模块导入错误，检查 Python 路径：

```bash
cd /var/www/geshixiugai
source venv/bin/activate
python -c "import sys; print(sys.path)"
python -c "from backend.app.main import app"
```

---

## 📋 完整排查命令

```bash
# 1. 查看错误日志
sudo tail -n 100 /var/log/geshixiugai/error.log

# 2. 手动测试启动（查看实时错误）
cd /var/www/geshixiugai
source venv/bin/activate
gunicorn -c gunicorn_config.py backend.app.main:app
```

---

**先执行查看错误日志命令，把错误信息发给我！** 🔍










