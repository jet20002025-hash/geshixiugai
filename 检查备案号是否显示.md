# 检查备案号是否显示

## 🔍 排查步骤

### 步骤 1：检查代码是否已更新

在服务器上执行：

```bash
cd /var/www/geshixiugai
grep -n "备案号" frontend/index.html
```

如果看到备案号相关代码，说明代码已更新。

### 步骤 2：检查服务是否已重启

```bash
# 检查服务状态
sudo systemctl status geshixiugai

# 查看服务启动时间
sudo systemctl show geshixiugai | grep ActiveEnterTimestamp
```

### 步骤 3：强制重启服务

```bash
sudo systemctl restart geshixiugai
sudo systemctl status geshixiugai
```

### 步骤 4：清除浏览器缓存

在浏览器中：
- 按 `Ctrl + Shift + R`（Windows/Linux）或 `Command + Shift + R`（Mac）强制刷新
- 或按 `F12` 打开开发者工具，右键刷新按钮，选择"清空缓存并硬性重新加载"

---

## 🚀 完整排查命令

```bash
# 1. 检查代码是否包含备案号
cd /var/www/geshixiugai
grep -n "备案号" frontend/index.html

# 2. 如果没找到，先修复权限并拉取代码
sudo chown -R admin:admin /var/www/geshixiugai
git config --global --add safe.directory /var/www/geshixiugai
git pull origin main

# 3. 确保 .env 权限正确
sudo chown nginx:nginx /var/www/geshixiugai/.env

# 4. 重启服务
sudo systemctl restart geshixiugai

# 5. 检查服务状态
sudo systemctl status geshixiugai
```

---

## 🔍 如果代码已更新但页面没显示

可能是浏览器缓存问题：

1. **强制刷新**：`Ctrl + Shift + R` 或 `Command + Shift + R`
2. **清除缓存**：浏览器设置 → 清除浏览数据 → 清除缓存
3. **无痕模式**：使用浏览器的无痕/隐私模式访问

---

**先执行检查命令，告诉我结果！** 🔍




