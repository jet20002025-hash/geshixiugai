# 域名无法访问排查

## 🔍 问题

DNS 已生效，但浏览器无法访问 http://geshixiugai.cn

---

## ✅ 排查步骤

### 步骤 1：检查服务器上的服务是否运行

在服务器上执行（阿里云控制台的对话框里）：

```bash
# 检查服务状态
sudo systemctl status geshixiugai

# 检查端口是否监听
sudo netstat -tlnp | grep 8000
```

### 步骤 2：检查 Nginx 是否运行

```bash
# 检查 Nginx 状态
sudo systemctl status nginx

# 检查 Nginx 配置
sudo nginx -t

# 查看 Nginx 错误日志
sudo tail -n 50 /var/log/nginx/geshixiugai_error.log
```

### 步骤 3：测试本地访问

```bash
# 测试本地访问
curl http://localhost:8000

# 测试 Nginx 代理
curl http://localhost
```

### 步骤 4：检查防火墙和安全组

```bash
# 检查防火墙
sudo firewall-cmd --list-ports
# 或
sudo iptables -L -n

# 检查 80 端口是否开放
sudo netstat -tlnp | grep 80
```

### 步骤 5：检查服务器安全组（阿里云控制台）

1. 登录阿里云控制台
2. 进入 ECS 实例
3. 检查安全组规则
4. 确保开放了 80 和 443 端口

---

## 🔧 常见问题和解决方案

### 问题 1：Nginx 未运行

```bash
# 启动 Nginx
sudo systemctl start nginx
sudo systemctl enable nginx
```

### 问题 2：Nginx 配置错误

```bash
# 检查配置
sudo nginx -t

# 如果配置有误，查看错误信息并修复
```

### 问题 3：安全组未开放 80 端口

在阿里云控制台：
1. ECS 实例 → 安全组
2. 添加规则：
   - 端口：80
   - 协议：TCP
   - 授权对象：0.0.0.0/0

### 问题 4：服务未运行

```bash
# 重启服务
sudo systemctl restart geshixiugai
sudo systemctl status geshixiugai
```

---

## 📋 完整排查命令

在服务器上执行：

```bash
# 1. 检查服务状态
sudo systemctl status geshixiugai

# 2. 检查 Nginx 状态
sudo systemctl status nginx

# 3. 测试本地访问
curl http://localhost:8000
curl http://localhost

# 4. 检查端口监听
sudo netstat -tlnp | grep 8000
sudo netstat -tlnp | grep 80

# 5. 查看 Nginx 错误日志
sudo tail -n 50 /var/log/nginx/geshixiugai_error.log
```

---

**先执行排查命令，告诉我结果！** 🔍










