# 备案号已添加 - 验证显示

## ✅ 代码已更新

代码中已包含备案号（第445行和448行），现在需要确保服务已重启。

---

## 🚀 操作步骤

### 步骤 1：重启服务

```bash
sudo systemctl restart geshixiugai
sudo systemctl status geshixiugai
```

### 步骤 2：清除浏览器缓存并刷新

在浏览器中：
1. **强制刷新**：
   - Windows/Linux：按 `Ctrl + Shift + R`
   - Mac：按 `Command + Shift + R`

2. **或清除缓存**：
   - 按 `F12` 打开开发者工具
   - 右键点击刷新按钮
   - 选择"清空缓存并硬性重新加载"

3. **或使用无痕模式**：
   - 打开浏览器的无痕/隐私模式
   - 访问 `https://geshixiugai.cn`

### 步骤 3：验证显示

在浏览器中：
- 访问：`https://geshixiugai.cn`
- 滚动到页面最底部
- 应该看到：**备案号：浙ICP备2025213303号**

---

## 📋 完整命令

```bash
# 重启服务
sudo systemctl restart geshixiugai

# 检查服务状态
sudo systemctl status geshixiugai

# 查看服务日志（确认服务正常运行）
sudo tail -n 20 /var/log/geshixiugai/error.log
```

---

## 🔍 如果还是看不到

### 检查前端文件是否正确加载

在浏览器中：
1. 按 `F12` 打开开发者工具
2. 切换到"网络"（Network）标签
3. 刷新页面
4. 查看 `index.html` 的响应内容
5. 检查是否包含备案号代码

### 检查 Nginx 配置

```bash
# 检查 Nginx 是否正常
sudo systemctl status nginx

# 查看 Nginx 错误日志
sudo tail -n 50 /var/log/nginx/geshixiugai_error.log
```

---

**先重启服务，然后强制刷新浏览器！** 🔄





