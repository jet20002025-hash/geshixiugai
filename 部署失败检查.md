# 部署失败检查清单

## 🔍 快速检查

### 1. 代码语法检查

```bash
cd /Users/zwj/word格式修改器
python3 -m py_compile backend/app/services/document_service.py
python3 -m py_compile backend/app/services/thesis_format_standard.py
```

### 2. 导入检查

```bash
python3 -c "
import sys
sys.path.insert(0, 'backend')
try:
    from app.services.document_service import DocumentService
    print('✅ DocumentService 导入成功')
except Exception as e:
    print(f'❌ 导入失败: {e}')
    import traceback
    traceback.print_exc()
"
```

### 3. 应用创建检查

```bash
python3 -c "
import sys
sys.path.insert(0, 'backend')
try:
    from app.main import create_app
    app = create_app()
    print('✅ 应用创建成功')
except Exception as e:
    print(f'❌ 应用创建失败: {e}')
    import traceback
    traceback.print_exc()
"
```

## 🐛 可能的问题

### 问题 1：参考文献段落可能没有 runs

**位置**：`document_service.py` 第905-907行

**问题**：如果参考文献段落是空的或者没有 runs，`para.runs` 可能为空，导致无法设置颜色。

**修复**：添加检查

```python
# 将参考文献段落标红
if para.runs:
    for run in para.runs:
        run.font.color.rgb = RGBColor(255, 0, 0)  # 红色
        run.font.bold = True  # 加粗
else:
    # 如果段落没有 runs，添加一个 run
    run = para.add_run(para.text if para.text else "")
    run.font.color.rgb = RGBColor(255, 0, 0)
    run.font.bold = True
```

### 问题 2：Vercel 配置问题

检查 `vercel.json` 是否正确：
- 配置项是否有效（Vercel 不支持 `maxRequestSize` 配置）
- Python 版本是否正确

### 问题 3：依赖问题

检查 `requirements.txt` 是否包含所有依赖。

## 📋 需要的信息

如果部署仍然失败，请提供：

1. **Vercel 构建日志**：
   - 登录 Vercel Dashboard
   - 进入项目 → Deployments
   - 点击失败的部署
   - 查看 "Build Logs"
   - 复制完整的错误信息

2. **错误类型**：
   - SyntaxError（语法错误）
   - ImportError（导入错误）
   - ModuleNotFoundError（模块未找到）
   - AttributeError（属性错误）
   - 其他错误

3. **错误位置**：
   - 文件名
   - 行号
   - 错误代码片段

---

**请提供 Vercel 构建日志中的具体错误信息，以便准确定位问题！**

