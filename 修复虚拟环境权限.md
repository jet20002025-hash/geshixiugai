# 修复虚拟环境权限问题

## 🔍 问题分析

虚拟环境目录权限不正确，导致无法安装包。

---

## ✅ 解决方案

### 方案 1：修复虚拟环境权限（推荐）

```bash
# 1. 修复虚拟环境目录权限
sudo chown -R $USER:$USER /var/www/geshixiugai/venv

# 或者如果当前用户是 admin，使用：
sudo chown -R admin:admin /var/www/geshixiugai/venv

# 2. 重新安装 gunicorn
cd /var/www/geshixiugai
source venv/bin/activate
pip install gunicorn
```

### 方案 2：使用 sudo 安装（临时方案）

```bash
cd /var/www/geshixiugai
source venv/bin/activate
sudo pip install gunicorn
```

### 方案 3：重新创建虚拟环境（如果方案1不行）

```bash
# 1. 删除旧虚拟环境
cd /var/www/geshixiugai
rm -rf venv

# 2. 重新创建虚拟环境
python3.11 -m venv venv

# 3. 激活虚拟环境
source venv/bin/activate

# 4. 安装所有依赖
pip install -r requirements.txt

# 5. 确保 gunicorn 已安装
pip install gunicorn
```

---

## 🚀 推荐操作流程

### 步骤 1：修复权限

```bash
# 检查当前用户
whoami

# 修复权限（替换 admin 为你的用户名）
sudo chown -R admin:admin /var/www/geshixiugai/venv
```

### 步骤 2：安装 gunicorn

```bash
cd /var/www/geshixiugai
source venv/bin/activate
pip install gunicorn
```

### 步骤 3：验证安装

```bash
gunicorn --version
which gunicorn
```

### 步骤 4：重启服务

```bash
deactivate
sudo systemctl restart geshixiugai
sudo systemctl status geshixiugai
```

---

## 📋 完整命令（复制粘贴）

```bash
# 1. 修复虚拟环境权限
sudo chown -R admin:admin /var/www/geshixiugai/venv

# 2. 进入项目目录
cd /var/www/geshixiugai

# 3. 激活虚拟环境
source venv/bin/activate

# 4. 安装 gunicorn
pip install gunicorn

# 5. 验证安装
gunicorn --version

# 6. 退出虚拟环境
deactivate

# 7. 重启服务
sudo systemctl restart geshixiugai

# 8. 检查状态
sudo systemctl status geshixiugai
```

---

## ⚠️ 如果方案1不行

如果修复权限后还是不行，使用方案3重新创建虚拟环境：

```bash
cd /var/www/geshixiugai
rm -rf venv
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pip install gunicorn
deactivate
sudo systemctl restart geshixiugai
```

---

**先执行权限修复命令！** 🔧








