# 服务启动失败排查 - 203/EXEC 错误

## 🔍 问题分析

错误代码 `203/EXEC` 通常表示：
- 可执行文件不存在
- 路径错误
- 权限问题

---

## ✅ 排查步骤

### 步骤 1：检查虚拟环境是否存在

```bash
# 检查虚拟环境目录
ls -la /var/www/geshixiugai/venv

# 检查 gunicorn 是否安装
ls -la /var/www/geshixiugai/venv/bin/gunicorn
```

### 步骤 2：检查 gunicorn 是否已安装

```bash
cd /var/www/geshixiugai
source venv/bin/activate
which gunicorn
gunicorn --version
```

### 步骤 3：检查 gunicorn_config.py 是否存在

```bash
ls -la /var/www/geshixiugai/gunicorn_config.py
```

### 步骤 4：手动测试启动

```bash
cd /var/www/geshixiugai
source venv/bin/activate
gunicorn -c gunicorn_config.py backend.app.main:app
```

---

## 🔧 解决方案

### 方案 1：安装 gunicorn（如果未安装）

```bash
cd /var/www/geshixiugai
source venv/bin/activate
pip install gunicorn
```

### 方案 2：检查并修复路径

检查 systemd 服务文件中的路径是否正确：

```bash
cat /etc/systemd/system/geshixiugai.service
```

确保路径正确：
- `WorkingDirectory=/var/www/geshixiugai`
- `ExecStart=/var/www/geshixiugai/venv/bin/gunicorn`

### 方案 3：重新安装依赖

```bash
cd /var/www/geshixiugai
source venv/bin/activate
pip install -r requirements.txt
```

---

## 🚀 快速修复命令

```bash
# 1. 进入项目目录
cd /var/www/geshixiugai

# 2. 激活虚拟环境
source venv/bin/activate

# 3. 安装 gunicorn（如果未安装）
pip install gunicorn

# 4. 验证安装
which gunicorn
gunicorn --version

# 5. 测试启动（按 Ctrl+C 停止）
gunicorn -c gunicorn_config.py backend.app.main:app

# 6. 如果测试成功，重启服务
sudo systemctl restart geshixiugai
sudo systemctl status geshixiugai
```

---

## 📋 完整排查流程

在服务器上依次执行：

```bash
# 1. 检查虚拟环境
ls -la /var/www/geshixiugai/venv/bin/gunicorn

# 2. 如果不存在，安装 gunicorn
cd /var/www/geshixiugai
source venv/bin/activate
pip install gunicorn

# 3. 检查配置文件
ls -la /var/www/geshixiugai/gunicorn_config.py

# 4. 手动测试
gunicorn -c gunicorn_config.py backend.app.main:app

# 5. 如果测试成功，重启服务
sudo systemctl restart geshixiugai
```

---

**先执行排查步骤，告诉我结果！** 🔍










