# 服务器上检查价格配置

## 🔍 问题

执行 `grep "DEFAULT_PRICE" backend/app/services/payment_service.py` 时提示文件不存在。

**原因**：当前不在项目目录下。

---

## ✅ 解决步骤

### 步骤 1：进入项目目录

```bash
# 进入项目目录
cd /var/www/geshixiugai

# 确认当前位置
pwd
# 应该显示：/var/www/geshixiugai
```

### 步骤 2：检查代码是否更新

```bash
# 检查代码中的默认价格
grep "DEFAULT_PRICE" backend/app/services/payment_service.py
# 应该显示：DEFAULT_PRICE = 0.09
```

### 步骤 3：拉取最新代码（如果还没有）

```bash
# 拉取最新代码
git pull origin main

# 再次检查
grep "DEFAULT_PRICE" backend/app/services/payment_service.py
```

### 步骤 4：检查 .env 文件

```bash
# 检查 .env 文件中的价格设置
cat .env | grep PAYMENT_PRICE
```

**如果显示 `PAYMENT_PRICE=0.99`**，需要修改：

```bash
# 编辑 .env 文件
vi .env

# 找到 PAYMENT_PRICE=0.99 这一行
# 修改为：PAYMENT_PRICE=0.09
# 保存退出（按 Esc，然后输入 :wq）
```

**如果没有显示**，说明没有设置，会使用默认值 0.09。

### 步骤 5：重启服务

```bash
# 重启服务
sudo systemctl restart geshixiugai

# 检查服务状态
sudo systemctl status geshixiugai
```

---

## 📋 完整命令（复制粘贴）

```bash
# 1. 进入项目目录
cd /var/www/geshixiugai

# 2. 拉取最新代码
git pull origin main

# 3. 检查代码中的默认价格
grep "DEFAULT_PRICE" backend/app/services/payment_service.py

# 4. 检查 .env 文件
cat .env | grep PAYMENT_PRICE

# 5. 如果 .env 中有 PAYMENT_PRICE=0.99，修改为 0.09
vi .env
# 找到 PAYMENT_PRICE=0.99，改为 PAYMENT_PRICE=0.09
# 保存退出（:wq）

# 6. 重启服务
sudo systemctl restart geshixiugai
```

---

## 🔍 验证

### 检查代码

```bash
cd /var/www/geshixiugai
grep "DEFAULT_PRICE" backend/app/services/payment_service.py
# 应该显示：DEFAULT_PRICE = 0.09
```

### 检查环境变量

```bash
cd /var/www/geshixiugai
cat .env | grep PAYMENT_PRICE
# 如果显示，应该是：PAYMENT_PRICE=0.09
# 如果没有显示，会使用默认值 0.09
```

---

## ⚠️ 重要提示

**必须先在项目目录下执行命令！**

当前目录：`~`（home目录）
项目目录：`/var/www/geshixiugai`

**先执行 `cd /var/www/geshixiugai` 再执行其他命令！**

---

**现在执行：先 `cd /var/www/geshixiugai`，然后执行其他命令！** 🚀







