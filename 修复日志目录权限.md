# 修复日志目录权限

## 🔍 问题

日志目录 `/var/log/geshixiugai/` 没有写权限。

---

## ✅ 解决方案

### 步骤 1：创建日志目录（如果不存在）

```bash
sudo mkdir -p /var/log/geshixiugai
```

### 步骤 2：修复日志目录权限

```bash
# 设置目录所有者为 www-data（或 nginx，根据系统）
sudo chown -R www-data:www-data /var/log/geshixiugai

# 或者如果是 CentOS/RHEL/Alinux，使用 nginx 用户
sudo chown -R nginx:nginx /var/log/geshixiugai
```

### 步骤 3：设置目录权限

```bash
sudo chmod 755 /var/log/geshixiugai
```

### 步骤 4：重启服务

```bash
sudo systemctl restart geshixiugai
sudo systemctl status geshixiugai
```

---

## 🚀 完整修复命令（复制粘贴）

```bash
# 1. 创建日志目录
sudo mkdir -p /var/log/geshixiugai

# 2. 修复权限（CentOS/RHEL/Alinux 使用 nginx）
sudo chown -R nginx:nginx /var/log/geshixiugai

# 3. 设置目录权限
sudo chmod 755 /var/log/geshixiugai

# 4. 验证权限
ls -la /var/log/geshixiugai

# 5. 重启服务
sudo systemctl restart geshixiugai

# 6. 检查服务状态
sudo systemctl status geshixiugai
```

---

## 🔍 如何确定使用哪个用户

检查 systemd 服务文件中指定的用户：

```bash
cat /etc/systemd/system/geshixiugai.service | grep User
```

如果显示 `User=www-data`，使用 `www-data`；如果显示 `User=nginx`，使用 `nginx`。

---

## ✅ 预期结果

修复后，服务应该能正常启动：

```bash
sudo systemctl status geshixiugai
```

应该显示：
```
Active: active (running)
```

---

**先执行权限修复命令！** 🔧








