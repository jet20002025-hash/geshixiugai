# 服务器更新代码 - 无需提交

## 🔍 问题说明

Git 检测到仓库所有者不匹配，这是正常的安全检测。

**解决方案**：不需要在服务器上提交代码，只需要拉取最新代码即可。

---

## ✅ 正确的操作

### 方法 1：直接拉取代码（推荐）

```bash
cd /var/www/geshixiugai
git pull origin main
sudo systemctl restart geshixiugai
```

### 方法 2：如果需要修复 Git 权限（可选）

如果以后需要在服务器上提交代码，可以执行：

```bash
git config --global --add safe.directory /var/www/geshixiugai
```

但**现在不需要**，因为代码已经在本地推送了。

---

## 🚀 现在执行

```bash
# 1. 进入项目目录
cd /var/www/geshixiugai

# 2. 拉取最新代码（包含备案号）
git pull origin main

# 3. 重启服务
sudo systemctl restart geshixiugai

# 4. 检查服务状态
sudo systemctl status geshixiugai
```

---

## ✅ 验证

在浏览器中访问 `https://geshixiugai.cn`，滚动到页面底部，应该看到备案号。

---

**现在执行 `git pull origin main` 即可！** 🚀


