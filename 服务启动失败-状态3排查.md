# 服务启动失败 - status=3 排查

## 🔍 问题分析

错误代码 `status=3/NOTIMPLEMENTED` 表示 gunicorn 可以运行，但应用启动时出错。

---

## ✅ 排查步骤

### 步骤 1：查看详细日志

```bash
# 查看服务日志
sudo journalctl -u geshixiugai -n 100 --no-pager

# 查看应用日志
sudo tail -f /var/log/geshixiugai/error.log
```

### 步骤 2：手动测试启动

```bash
cd /var/www/geshixiugai
source venv/bin/activate
gunicorn -c gunicorn_config.py backend.app.main:app
```

查看具体错误信息。

### 步骤 3：检查依赖是否完整

```bash
cd /var/www/geshixiugai
source venv/bin/activate
pip install -r requirements.txt
```

### 步骤 4：检查 Python 模块导入

```bash
cd /var/www/geshixiugai
source venv/bin/activate
python -c "import backend.app.main"
```

---

## 🔧 常见问题和解决方案

### 问题 1：缺少依赖包

```bash
cd /var/www/geshixiugai
source venv/bin/activate
pip install -r requirements.txt
```

### 问题 2：gunicorn_config.py 配置问题

检查配置文件：

```bash
cat /var/www/geshixiugai/gunicorn_config.py
```

### 问题 3：工作目录问题

检查 systemd 服务文件中的工作目录：

```bash
cat /etc/systemd/system/geshixiugai.service
```

确保 `WorkingDirectory=/var/www/geshixiugai`

### 问题 4：环境变量问题

检查 .env 文件是否正确加载：

```bash
cat /var/www/geshixiugai/.env
```

---

## 🚀 快速修复流程

```bash
# 1. 查看详细错误
sudo journalctl -u geshixiugai -n 100 --no-pager

# 2. 手动测试启动（查看具体错误）
cd /var/www/geshixiugai
source venv/bin/activate
gunicorn -c gunicorn_config.py backend.app.main:app

# 3. 如果看到具体错误，根据错误信息修复
# 例如：缺少依赖，安装依赖
pip install -r requirements.txt

# 4. 测试成功后，重启服务
deactivate
sudo systemctl restart geshixiugai
```

---

**先执行查看日志命令，告诉我具体的错误信息！** 🔍


