# 诊断日志没有输出的排查步骤

如果执行 `sudo grep "\[诊断\]" /var/log/geshixiugai/error.log | tail -n 50` 没有任何输出，请按以下步骤排查：

## 🔍 排查步骤

### 步骤1：检查日志文件是否存在

```bash
# 检查日志文件
sudo ls -lh /var/log/geshixiugai/error.log
```

**可能的情况：**
- ✅ 文件存在且有内容 → 继续步骤2
- ❌ 文件不存在 → 执行步骤4（创建日志目录）
- ⚠️ 文件存在但为空 → 继续步骤2

---

### 步骤2：检查日志文件内容

```bash
# 查看日志文件的最后20行
sudo tail -n 20 /var/log/geshixiugai/error.log
```

**如果没有任何输出：**
- 说明日志文件为空，服务可能还没有处理过文档
- 需要上传一个文档来触发诊断

---

### 步骤3：检查代码是否已更新

```bash
# 检查代码是否包含诊断功能
cd /var/www/geshixiugai
sudo grep "_diagnose_integrity_abstract_separation" backend/app/services/document_service.py
```

**如果没有任何输出：**
- 说明代码还没有更新，需要执行：
  ```bash
  cd /var/www/geshixiugai
  git pull origin main
  sudo systemctl restart geshixiugai
  ```

---

### 步骤4：确保日志目录存在且有权限

```bash
# 创建日志目录（如果不存在）
sudo mkdir -p /var/log/geshixiugai

# 设置权限（根据系统用户调整）
# CentOS/RHEL/Alinux 使用 nginx
sudo chown -R nginx:nginx /var/log/geshixiugai

# Ubuntu/Debian 使用 www-data
# sudo chown -R www-data:www-data /var/log/geshixiugai

# 设置目录权限
sudo chmod 755 /var/log/geshixiugai
```

---

### 步骤5：重启服务

```bash
# 重启服务以加载新代码
sudo systemctl restart geshixiugai

# 检查服务状态
sudo systemctl status geshixiugai
```

---

### 步骤6：上传文档触发诊断

诊断功能只在处理文档时才会运行，所以需要：

1. **访问网站并上传一个文档**
2. **等待文档处理完成**
3. **然后查看诊断日志**

---

## 🚀 快速排查命令

```bash
# 1. 检查日志文件
sudo ls -lh /var/log/geshixiugai/error.log

# 2. 查看日志内容
sudo tail -n 20 /var/log/geshixiugai/error.log

# 3. 检查代码是否已更新
cd /var/www/geshixiugai
git pull origin main

# 4. 重启服务
sudo systemctl restart geshixiugai

# 5. 检查服务状态
sudo systemctl status geshixiugai
```

---

## 📋 使用检查脚本

项目根目录有一个检查脚本，可以直接运行：

```bash
# 在服务器上执行
cd /var/www/geshixiugai
./检查诊断日志.sh
```

或者如果脚本在本地，上传到服务器后执行。

---

## ⚠️ 重要提示

**诊断日志只在处理文档时才会生成！**

如果没有诊断日志输出，可能是因为：
1. ✅ **代码还没有更新** → 执行 `git pull origin main` 和 `sudo systemctl restart geshixiugai`
2. ✅ **还没有处理过文档** → 上传一个文档进行测试
3. ✅ **日志文件权限问题** → 检查并修复日志目录权限

---

## 🎯 完整操作流程

```bash
# 1. 更新代码
cd /var/www/geshixiugai
git pull origin main

# 2. 确保日志目录存在
sudo mkdir -p /var/log/geshixiugai
sudo chown nginx:nginx /var/log/geshixiugai  # 或 www-data:www-data
sudo chmod 755 /var/log/geshixiugai

# 3. 重启服务
sudo systemctl restart geshixiugai

# 4. 检查服务状态
sudo systemctl status geshixiugai

# 5. 上传一个文档进行测试（在网页上操作）

# 6. 查看诊断日志
sudo grep "\[诊断\]" /var/log/geshixiugai/error.log | tail -n 50
```

---

**执行完这些步骤后，再上传一个文档，然后查看诊断日志！** 🔧

