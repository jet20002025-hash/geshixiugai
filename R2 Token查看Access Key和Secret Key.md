# R2 Token 查看 Access Key 和 Secret Key

## 🔍 问题

在"编辑令牌"页面看不到 Access Key ID 和 Secret Access Key，这是正常的（安全考虑）。

---

## ✅ 解决方法

### 方法 1：查看 Token 列表（如果刚创建）

1. **点击左上角的返回箭头**（←）或 "取消" 按钮
2. **回到 API Tokens 列表页面**
3. **找到你刚创建的 Token**：`word-formatter-r2-token`
4. **点击 Token 名称**进入详情页
5. **查看是否显示 Access Key ID 和 Secret Access Key**

**注意**：Secret Access Key 通常只显示一次，如果已经关闭过页面，可能看不到了。

---

### 方法 2：重新创建 Token（推荐）

如果 Secret Access Key 已经看不到了，需要重新创建：

1. **删除当前 Token**：
   - 在 API Tokens 列表页面
   - 找到 `word-formatter-r2-token`
   - 点击删除按钮

2. **重新创建 Token**：
   - 点击 "Create API Token"
   - 选择 "Custom token"
   - 配置权限（和之前一样）：
     - Account → Workers R2 存储 → Edit
   - 填写 Token name：`word-formatter-r2-token`
   - 配置账号资源：Include → 选择你的账号
   - 配置域名资源：None
   - 点击 "Create Token"

3. **立即复制保存两个值**：
   - **Access Key ID**：第一个值
   - **Secret Access Key**：第二个值（只显示一次！）

---

## 📋 需要收集的 3 个值

配置 Vercel 需要以下 3 个值：

### 1. Account ID
- **位置**：R2 Dashboard 右侧 "Account Details"
- **格式**：32 个字符，类似 `5003936d05d7bc23217a0fdef979a238`
- **操作**：点击复制图标复制

### 2. Access Key ID
- **位置**：Token 创建成功页面显示的第一个值
- **格式**：通常 20 个字符
- **可以随时查看**（在 Token 列表页面）

### 3. Secret Access Key
- **位置**：Token 创建成功页面显示的第二个值
- **格式**：通常 40 个字符
- **只显示一次**，关闭页面后就看不到了

---

## 🎯 立即操作

### 步骤 1：获取 Account ID

1. **回到 R2 Dashboard**：https://dash.cloudflare.com
2. **点击左侧菜单 "R2 对象存储"**
3. **查看右侧边栏 "Account Details"**
4. **找到 "Account ID"**
5. **点击复制图标**，复制 Account ID

### 步骤 2：重新创建 Token（获取 Access Key 和 Secret Key）

1. **访问 API Tokens 页面**：https://dash.cloudflare.com/profile/api-tokens
2. **删除旧的 Token**（如果存在）
3. **创建新 Token**：
   - 点击 "Create API Token"
   - 选择 "Custom token"
   - 配置权限：Account → Workers R2 存储 → Edit
   - Token name：`word-formatter-r2-token`
   - Account Resources：Include → 选择你的账号
   - Zone Resources：None
   - 点击 "Create Token"
4. **立即复制保存**：
   - Access Key ID（第一个值）
   - Secret Access Key（第二个值）

---

## 📝 收集齐 3 个值后

收集齐以下 3 个值后，告诉我：

1. ✅ **Account ID** = `你的 Account ID`
2. ✅ **Access Key ID** = `你的 Access Key ID`
3. ✅ **Secret Access Key** = `你的 Secret Access Key`

然后我会指导你配置 Vercel 环境变量！

---

## 💡 提示

- **Secret Access Key 只显示一次**，创建后立即复制保存
- **如果丢失了 Secret Access Key**，必须删除 Token 重新创建
- **Account ID 可以随时查看**，在 R2 Dashboard 右侧

**现在请按照上面的步骤操作，收集齐 3 个值后告诉我！**

