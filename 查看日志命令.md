# 查看日志命令

## 基本查看命令

### 1. 查看检测日志
```bash
sudo grep "\[检测\]" /var/log/geshixiugai/error.log | tail -n 50
```

### 2. 查看诊断日志
```bash
sudo grep "\[诊断\]" /var/log/geshixiugai/error.log | tail -n 50
```

### 3. 查看修复日志
```bash
sudo grep "\[修复\]" /var/log/geshixiugai/error.log | tail -n 50
```

### 4. 查看完整的诊断流程（检测+诊断+修复）
```bash
sudo grep "\[检测\]\|\[诊断\]\|\[修复\]" /var/log/geshixiugai/error.log | tail -n 100
```

## 特定内容查看

### 5. 查看PDF分页结果（包含页码信息）
```bash
sudo grep "PDF分页结果\|诚信承诺所在页码\|摘要所在页码\|页码对比" /var/log/geshixiugai/error.log | tail -n 30
```

### 6. 查看修复前后的检测结果
```bash
sudo grep "修复前检测\|修复后检测" /var/log/geshixiugai/error.log | tail -n 30
```

### 7. 查看查找诚信承诺和摘要的过程
```bash
sudo grep "找到诚信承诺\|找到中文摘要\|开始查找" /var/log/geshixiugai/error.log | tail -n 30
```

## 实时查看

### 8. 实时查看检测日志（处理文档时）
```bash
sudo tail -f /var/log/geshixiugai/error.log | grep "\[检测\]"
```

### 9. 实时查看诊断日志
```bash
sudo tail -f /var/log/geshixiugai/error.log | grep "\[诊断\]"
```

### 10. 实时查看修复日志
```bash
sudo tail -f /var/log/geshixiugai/error.log | grep "\[修复\]"
```

### 11. 实时查看所有相关日志
```bash
sudo tail -f /var/log/geshixiugai/error.log | grep "\[检测\]\|\[诊断\]\|\[修复\]"
```

## 按时间查看

### 12. 查看今天的日志
```bash
sudo grep "\[检测\]" /var/log/geshixiugai/error.log | grep "$(date '+%Y-%m-%d')" | tail -n 50
```

### 13. 查看最近1小时的日志
```bash
sudo grep "\[检测\]" /var/log/geshixiugai/error.log | grep "$(date '+%Y-%m-%d %H')" | tail -n 50
```

## 统计信息

### 14. 统计检测日志数量
```bash
sudo grep -c "\[检测\]" /var/log/geshixiugai/error.log
```

### 15. 统计诊断日志数量
```bash
sudo grep -c "\[诊断\]" /var/log/geshixiugai/error.log
```

### 16. 统计修复日志数量
```bash
sudo grep -c "\[修复\]" /var/log/geshixiugai/error.log
```

## 查看最近的日志（不筛选）

### 17. 查看最近的日志（最后50行）
```bash
sudo tail -n 50 /var/log/geshixiugai/error.log
```

### 18. 查看最近的日志（最后100行）
```bash
sudo tail -n 100 /var/log/geshixiugai/error.log
```

## 使用脚本查看

### 19. 使用查看检测日志脚本
```bash
cd /var/www/geshixiugai
chmod +x 查看检测日志.sh
./查看检测日志.sh
```

### 20. 使用查看诊断结果脚本
```bash
cd /var/www/geshixiugai
chmod +x 查看诊断结果.sh
./查看诊断结果.sh
```

## 快速命令组合

### 21. 一键查看所有相关日志（最后100条）
```bash
sudo grep "\[检测\]\|\[诊断\]\|\[修复\]" /var/log/geshixiugai/error.log | tail -n 100
```

### 22. 查看PDF页码信息
```bash
sudo grep "📄\|页码\|PDF总页数" /var/log/geshixiugai/error.log | tail -n 30
```

### 23. 查看错误和警告
```bash
sudo grep "❌\|⚠️\|错误\|失败" /var/log/geshixiugai/error.log | tail -n 50
```

## 保存日志到文件

### 24. 保存检测日志到文件
```bash
sudo grep "\[检测\]" /var/log/geshixiugai/error.log | tail -n 100 > ~/检测日志.txt
```

### 25. 保存完整日志到文件
```bash
sudo grep "\[检测\]\|\[诊断\]\|\[修复\]" /var/log/geshixiugai/error.log | tail -n 200 > ~/完整日志.txt
```

