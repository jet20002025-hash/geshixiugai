# Nginx 配置文件模板
# 位置：/etc/nginx/sites-available/geshixiugai
# 使用方法：复制到服务器并启用

server {
    listen 80;
    server_name geshixiugai.cn www.geshixiugai.cn;

    # 设置请求体大小限制（50MB，可根据需要调整）
    client_max_body_size 50m;

    # 增加超时时间（用于大文件上传）
    proxy_connect_timeout 300s;
    proxy_send_timeout 300s;
    proxy_read_timeout 300s;

    # 日志
    access_log /var/log/nginx/geshixiugai_access.log;
    error_log /var/log/nginx/geshixiugai_error.log;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 支持（如果需要）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# SSL 配置（Certbot 会自动添加）
# 申请证书后，Certbot 会自动修改此文件，添加 SSL 配置



