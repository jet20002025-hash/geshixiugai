# 更新代码到服务器

## ✅ 已完成的修改

1. **价格改为 0.99 元** ✅
2. **移除模拟支付选项** ✅
3. **支付宝支付成功后显示下载按钮** ✅
4. **修复前端代码中的 mock 残留** ✅

---

## 🚀 更新步骤

### 步骤 1：提交代码到 GitHub（在你的电脑上）

```bash
# 在你的电脑上执行
cd /Users/zwj/word格式修改器

# 添加修改的文件
git add backend/app/services/payment_service.py
git add backend/app/api/payments.py
git add backend/app/api/documents.py
git add backend/app/schemas/documents.py
git add frontend/index.html

# 提交
git commit -m "修改支付价格为0.99元，移除模拟支付，优化支付宝支付成功后的下载按钮显示"

# 推送到 GitHub
git push origin main
```

### 步骤 2：在服务器上拉取代码

```bash
# 在服务器上执行（使用阿里云控制台网页终端或SSH）
cd /var/www/geshixiugai

# 拉取最新代码
git pull origin main
```

### 步骤 3：重启服务

```bash
# 重启服务
sudo systemctl restart geshixiugai

# 检查服务状态
sudo systemctl status geshixiugai
```

---

## ✅ 验证

### 1. 检查价格

访问网站，上传文档后，应该看到：
- 支付金额：**¥0.99**（不是 ¥9.90）

### 2. 检查支付方式

应该只看到：
- ✅ **支付宝**（没有模拟支付选项）

### 3. 测试支付流程

1. 上传文档
2. 选择支付宝支付
3. 完成支付
4. 跳转回网站后，应该：
   - ✅ 显示"支付成功！可以下载正式版文档了。"
   - ✅ 显示"下载正式版（已付费）"按钮
   - ✅ 点击按钮可以下载文档

---

## 📋 完整命令（复制粘贴）

### 在你的电脑上（步骤 1）

```bash
cd /Users/zwj/word格式修改器
git add backend/app/services/payment_service.py backend/app/api/payments.py backend/app/api/documents.py backend/app/schemas/documents.py frontend/index.html
git commit -m "修改支付价格为0.99元，移除模拟支付，优化支付宝支付成功后的下载按钮显示"
git push origin main
```

### 在服务器上（步骤 2 和 3）

```bash
cd /var/www/geshixiugai
git pull origin main
sudo systemctl restart geshixiugai
sudo systemctl status geshixiugai
```

---

## 🎯 修改说明

### 1. 价格修改

- **文件**：`backend/app/services/payment_service.py`
- **修改**：`DEFAULT_PRICE = 0.99`

### 2. 移除模拟支付

- **文件**：`backend/app/services/payment_service.py`、`backend/app/api/payments.py`、`frontend/index.html`
- **修改**：从支付方式列表中移除 `"mock"`

### 3. 支付成功后显示下载按钮

- **文件**：`frontend/index.html`、`backend/app/api/documents.py`、`backend/app/schemas/documents.py`
- **修改**：
  - API 返回 `download_token`（仅已支付时）
  - 前端检测支付成功后自动显示下载按钮

---

## ⚠️ 注意事项

1. **确保 SSH 密钥已配置**（之前已经配置好了 ✅）
2. **确保服务重启成功**
3. **测试支付流程**确保正常工作

---

**现在执行：先在你的电脑上提交代码，然后在服务器上拉取并重启服务！** 🚀







