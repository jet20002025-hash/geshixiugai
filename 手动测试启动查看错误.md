# 手动测试启动查看具体错误

## 🔍 问题

服务日志没有显示具体错误信息，需要手动测试启动来查看。

---

## ✅ 手动测试启动

### 步骤 1：进入项目目录并激活虚拟环境

```bash
cd /var/www/geshixiugai
source venv/bin/activate
```

### 步骤 2：手动启动应用（查看具体错误）

```bash
gunicorn -c gunicorn_config.py backend.app.main:app
```

**这会显示具体的错误信息**，请把完整的错误信息发给我。

### 步骤 3：如果看到错误，根据错误修复

常见错误：
- **缺少依赖**：`pip install -r requirements.txt`
- **模块导入错误**：检查 Python 路径
- **配置文件错误**：检查 `.env` 文件
- **存储配置错误**：检查 OSS 配置

---

## 🔧 替代测试方法

如果 gunicorn 启动有问题，也可以直接用 Python 测试：

```bash
cd /var/www/geshixiugai
source venv/bin/activate
python -c "from backend.app.main import app; print('OK')"
```

或者：

```bash
cd /var/www/geshixiugai
source venv/bin/activate
python -m backend.app.main
```

---

## 📋 完整测试命令

```bash
# 1. 进入项目目录
cd /var/www/geshixiugai

# 2. 激活虚拟环境
source venv/bin/activate

# 3. 测试导入
python -c "from backend.app.main import app; print('Import OK')"

# 4. 手动启动 gunicorn（查看具体错误）
gunicorn -c gunicorn_config.py backend.app.main:app
```

---

**先执行手动启动命令，把完整的错误信息发给我！** 🔍








