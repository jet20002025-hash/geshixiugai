# 支付宝配置完成 - 验证

## ✅ 配置已完成

你已经填写好了支付宝配置，现在需要验证配置是否正确。

---

## 🚀 验证步骤

### 步骤 1：确保文件权限正确

在服务器上执行（阿里云控制台的对话框里）：

```bash
# 确保 .env 文件权限正确（服务需要读取）
sudo chown nginx:nginx /var/www/geshixiugai/.env
sudo chmod 600 /var/www/geshixiugai/.env
```

### 步骤 2：重启服务

```bash
sudo systemctl restart geshixiugai
sudo systemctl status geshixiugai
```

### 步骤 3：查看日志确认配置加载

```bash
# 查看日志，确认支付宝配置加载成功
sudo tail -n 50 /var/log/geshixiugai/error.log | grep -i alipay
```

应该看到：`[AlipayService] 支付宝客户端初始化成功`

---

## ✅ 验证配置

### 方法 1：访问调试端点（推荐）

在浏览器中访问：
```
https://geshixiugai.cn/payments/debug/config
```

应该返回 JSON，包含支付宝配置信息：
```json
{
  "alipay": {
    "app_id_exists": true,
    "app_id_preview": "2021****",
    "private_key_exists": true,
    "private_key_length": 数字,
    "private_key_has_begin": true/false,
    "public_key_exists": true,
    "public_key_length": 数字,
    "public_key_has_begin": true/false,
    "sign_type": "RSA2",
    "gateway": "https://openapi.alipay.com/gateway.do",
    "configured": true
  },
  "available_payment_methods": ["mock", "alipay", ...]
}
```

**检查要点**：
- ✅ `app_id_exists: true` - AppID 已配置
- ✅ `private_key_exists: true` - 私钥已配置
- ✅ `public_key_exists: true` - 公钥已配置
- ✅ `configured: true` - 配置完整
- ✅ `alipay` 在 `available_payment_methods` 中 - 支付方式可用

### 方法 2：测试支付流程

1. **访问网站**：https://geshixiugai.cn
2. **上传模板和文档**：完成文档处理
3. **进入支付页面**：查看支付信息
4. **选择支付方式**：应该能看到"支付宝"选项
5. **点击"立即支付"**：应该跳转到支付宝支付页面

---

## 📋 完整验证命令（复制粘贴）

```bash
# 1. 确保文件权限正确
sudo chown nginx:nginx /var/www/geshixiugai/.env
sudo chmod 600 /var/www/geshixiugai/.env

# 2. 重启服务
sudo systemctl restart geshixiugai

# 3. 检查服务状态
sudo systemctl status geshixiugai

# 4. 查看日志
sudo tail -n 50 /var/log/geshixiugai/error.log | grep -i alipay
```

---

## 🔍 如果配置未生效

### 检查配置格式

```bash
# 检查配置项是否存在（隐藏值）
sudo grep "^ALIPAY" /var/www/geshixiugai/.env | sed 's/=.*/=***/'
```

应该看到：
```
ALIPAY_APP_ID=***
ALIPAY_PRIVATE_KEY=***
ALIPAY_PUBLIC_KEY=***
ALIPAY_SIGN_TYPE=***
ALIPAY_GATEWAY=***
```

### 常见问题

1. **配置格式错误**：
   - 确保没有多余的空格
   - 确保值没有用引号包裹（除非值本身包含空格）
   - 确保每行一个配置项

2. **私钥/公钥格式问题**：
   - 如果私钥包含 `-----BEGIN RSA PRIVATE KEY-----`，需要完整复制
   - 如果是纯 base64 字符串，直接粘贴即可

3. **服务未重启**：
   - 配置修改后必须重启服务才能生效

---

## ✅ 配置检查清单

- [ ] .env 文件权限正确（nginx:nginx, 600）
- [ ] 服务已重启
- [ ] 日志显示支付宝客户端初始化成功
- [ ] 调试端点显示 `configured: true`
- [ ] 支付页面显示"支付宝"选项
- [ ] 点击支付能跳转到支付宝支付页面

---

**现在执行验证命令！** 🔍




